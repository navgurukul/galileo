describe('Courses Services', function () {
	const server = {
		services: jest.fn(),
		models: jest.fn(),
		errors: jest.fn(),
		knex: jest.fn(),
	};
	it('should do something', function () {
		// const coursesService = new CoursesService(server, {});
	});
});
